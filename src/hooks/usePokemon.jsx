/*export const usePokemon = () => {
  const [arregloPokemones, setArregloPokemones] = useState([])
 
  const handleGetPokemon = async(name, e) => {

      e.preventDefault()

      await reqGif(name).then((arregloPokemones)=>{
        setArregloPokemones(arregloPokemones)
      })
  }
  
  
  return {
    handleGetPokemon,
    arregloGifs
  }
}*/
