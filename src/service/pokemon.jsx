/*import React from 'react'
const id = ""
const limit = 1

export const reqPokemon = async(nombre) => {
    try {
        const resp = await fetch(`https://pokeapi.co/api/v2/pokemon/api_key=${id}&q=${nombre}&limit=${limit}`)
        const {data} = await resp.json()

        const arregloPokemones = data.map((pokemon)=>
            ({
            /*id: pokemon.id,
            name: pokemon.forms.name,
            url: pokemon.sprites.other["official-artwork"].front_default
        })
        )

        console.log(data)
        
        //return arregloGifs

    }catch (err) {
        console.log(err)
    }
}*/
